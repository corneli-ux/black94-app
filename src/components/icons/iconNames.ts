/**
 * Icon name mapping — reference for migrating Ionicons → AppIcon (MaterialIcons).
 *
 * Left: Old Ionicons name    Right: New MaterialIcons name (for AppIcon component)
 *
 * Navigation
 *   arrow-back              → arrow-back
 *   chevron-forward         → chevron-right
 *   menu                    → menu
 *
 * Actions
 *   close                   → close
 *   close-circle            → cancel
 *   send                    → send
 *   add                     → add
 *   add-circle              → add-circle
 *   add-circle-outline      → add-circle-outline
 *   create-outline          → edit
 *   checkmark               → check
 *   checkmark-circle        → check-circle
 *   checkmark-done-outline  → task-alt        (MaterialCommunityIcons)
 *   ellipsis-horizontal     → more-horiz
 *
 * Social
 *   heart                   → favorite
 *   heart-outline           → favorite-border
 *   chatbubble              → chat
 *   chatbubble-outline      → chat-bubble-outline
 *   chatbubble-ellipses-outline → forum         (MaterialCommunityIcons)
 *   share-outline           → share
 *   share-social-outline     → share            (MaterialCommunityIcons)
 *   notifications           → notifications
 *   notifications-outline   → notifications-outlined
 *   person-add              → person-add
 *   people-outline          → groups
 *   person-outline          → person-outline
 *
 * Content
 *   bookmark                → bookmark
 *   bookmark-outline        → bookmark-border
 *   trending-up-outline     → trending-up
 *   images-outline          → photo-library
 *   image-outline           → image
 *   newspaper-outline       → article          (MaterialCommunityIcons)
 *   document-text-outline   → description
 *
 * Search & Filter
 *   search                  → search
 *   search-outline          → search
 *
 * Settings & Privacy
 *   settings-outline        → settings
 *   lock-closed-outline     → lock-outline      (MaterialCommunityIcons)
 *   shield-checkmark-outline → verified-user    (MaterialCommunityIcons)
 *   eye                     → visibility
 *   eye-off / eye-off-outline → visibility-off
 *   key-outline             → vpn-key           (MaterialCommunityIcons)
 *
 * Communication
 *   mail-outline            → mail-outline
 *   at-outline              → alternate-email
 *   link-outline            → link
 *
 * Moderation
 *   ban-outline             → block
 *   volume-mute-outline     → volume-off
 *   trash-outline           → delete-outline
 *
 * Account
 *   log-out-outline         → logout
 *   phone-portrait-outline  → phone-android
 *   download-outline        → download
 *   calendar-outline        → calendar-today
 *
 * Media
 *   images-outline          → photo-library
 *   image-outline           → image
 *   card-outline            → credit-card      (MaterialCommunityIcons)
 *   chatbubbles-outline     → forum            (MaterialCommunityIcons)
 *
 * Brand / Premium
 *   diamond                 → diamond          (MaterialCommunityIcons)
 *   diamond-outline         → diamond          (MaterialCommunityIcons, opacity-based)
 *   sunny-outline           → wb-sunny
 *
 * Anonymous
 *   incognito               → incognito        (MaterialCommunityIcons)
 *
 * Misc
 *   alert-circle            → error-outline
 *   cube-outline            → inventory-2
 *   more-horiz              → more-horiz
 *
 * NOTE: When MaterialIcons doesn't have an icon, use MaterialCommunityIcons.
 * The AppIcon component auto-detects and routes to the correct library.
 */
