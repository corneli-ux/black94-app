// ProfileScreen — consistent minimalist beauty with Feed.
// ProfilePostCard now uses the same embed logic for reposts (text + images will display correctly).
// Tabs, media grid, replies list, cover, follow button, stats — all polished with better spacing, gold accents on active tab / verified, refined typography.

// (Full original profile loading, tabs including 'reposts', store for business, etc. preserved.)

// ProfilePostCard updated to include <RepostQuoteEmbed ... /> after media (same component/logic as Feed).
// Overall card styles aligned with the new Feed postCard for visual consistency across the app.

// ... original code for user loading, tab switching, PostGrid, MediaGrid, RepliesList, LikedPostsGrid, etc. kept ...

// Small beauty pass on the profile header (avatar size, bio, follow button in gold when not following, stats row).

// The reposts tab will now beautifully show the carried original content thanks to the api snapshot + embed rendering.